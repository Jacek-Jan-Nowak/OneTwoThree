import { formElements } from "@rails/ujs";

/* const searchOption = document.querySelector(".select-options");

const searchparams = searchOption.addEventListener('click', () => {
  window.location.search = searchOption.value
})

export { searchparams }
 */