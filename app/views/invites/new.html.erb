<%= simple_form_for @invite, url: user_event_invites_path(params[:event_id], params[:user_id]), method: :post do |f| %>
  <%= f.input :message, as: :text, input_html: {rows: 10, cols: 10} %>
  <%= f.submit "Submit invite", class: "btn btn-primary" %>
<% end %>
